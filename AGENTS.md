# AGENTS.md — astra-skill-execution-framework

> **For AI coding agents working on this project.**
> Read this first before making changes.

## Project North Star

An optional, manually-invoked keyword-based recommendation tool that suggests
relevant skills and capability chains for a given task description.

**This is no longer part of the agentic harness.**  The `work-principles` plugin
has replaced the execution-framework's routing role with phase-based gates
and tool-triggered auto-loading.  The execution-framework now exists purely as
a reference tool for manual debugging and ad-hoc exploration.

## Architecture (3 Layers, no Trigger)

| Layer | File | Purpose |
|:------|:-----|:--------|
| **Discovery** | `scripts/sync-routing.py --scan` | Scan skill directories, build index |
| **Index** | `skills_index.json` (generated) | Skill metadata: name, tags, indicators, description |
| **Recommend** | `scripts/recommend.py` | Match task description → ranked skill chain |

There is no auto-trigger layer.  The agent is not required to call recommend.py
before acting — the `work-principles` plugin handles research gating via the
`[HARNESS:]` marker system and tool-whitelist enforcement.

## Key Files

| File | Role | Edit With Care? |
|:-----|:-----|:----------------|
| `routing.yaml` | **Single source of truth** for routing patterns. Maps task language → capability chains. | ✅ Yes |
| `scripts/recommend.py` | Reads routing.yaml + skills_index.json, does keyword matching, prints recommendation | ⚠️ Low priority |
| `scripts/sync-routing.py` | Scans skill directories, generates skills_index.json | ⚠️ Rarely changes |
| `skills_index.json` | Auto-generated by `--scan`. Do not edit manually. | ❌ Never hand-edit |
| `SKILL.md` | Hermes agent instructions for when the skill is loaded | ⚠️ Update if routing.yaml changes |
| `config.yaml` | Declares scan paths for --scan mode | ⚠️ Rarely changes |

## Design Decisions

### 1. routing.yaml as single source of truth

Not SKILL.md, not README.md.  All routing logic lives in structured YAML so it's:
- Machine-readable (recommend.py parses it)
- Versionable (independent of prose docs)
- Editable (one file, clear schema)

### 2. Language-independent matching

Keywords in `when:` are bilingual (Chinese + English) heuristics, NOT an exhaustive list.  The agent should use semantic understanding first, fall back to keywords.

### 3. workflow ≠ background

| | workflow | background |
|:--|:---------|:-----------|
| Checkable? | ✅ Yes — appended to todo, can be ticked off | ❌ No — always active |
| Ordered? | ✅ Sequential steps | ❌ Parallel rules |
| Skip? | Optional steps skipped if `when:` doesn't match | Never skip |
| Example | `pre-action-research → credential → change-safeguard → closure` | "诚实优先", "先保全再改" |

### 4. No auto-trigger (by design)

The execution-framework no longer has a plugin or SOUL.md trigger.  The
`work-principles` plugin uses phase gates (Research, Modify, Closure) and
tool-triggered auto-skill loading instead of routing tables.  This repo
remains available for manual `recommend.py` calls when the agent wants a
quick suggestion.

### 5. Scope boundaries

| In scope | Out of scope |
|:---------|:-------------|
| Providing keyword-based skill suggestions | Harness enforcement (handled by work-principles) |
| Maintaining routing.yaml as a reference index | Phase state machine (handled by work-principles) |
| Index generation and sync | Tool-triggered auto-loading (handled by work-principles) |
| Manual debugging / ad-hoc exploration | Agent task classification (handled by [HARNESS:] markers) |

## How to Contribute

### Adding a new capability to routing.yaml

```yaml
- id: my-capability
  label: "中文标签"
  order: N
  when:
    - trigger1
    - 触发词2
  skills:
    - name: target-skill
```

1. Add the entry to `routing.yaml` under the correct `workflow` or `background` section
2. Run `python3 scripts/sync-routing.py --scan --write-index` to regenerate the index
3. Test with `python3 scripts/recommend.py "test phrase"` — verify it appears in the output

### Testing recommendations

```bash
# Test a specific task
python3 scripts/recommend.py "帮我把 Nginx 配置成反向代理"

# Test with structured output
python3 scripts/recommend.py --json "push this repo to GitHub"

# Interactive mode (type tasks repeatedly)
python3 scripts/recommend.py --interactive
```

### Before committing

- [ ] `routing.yaml` parses as valid YAML (`python3 -c "import yaml; yaml.safe_load(open('routing.yaml'))"`)
- [ ] `recommend.py "test phrase"` returns sensible output (not empty/crash)
- [ ] New keywords in `when:` have both Chinese and English variants where applicable
- [ ] `always: true` steps (context, closure) are never accidentally removed
- [ ] skills_index.json is regenerated if skills_index entries changed

## Related Projects

- [astra-aiagent-infra](https://github.com/alrcatraz/astra-aiagent-infra) — ecosystem registry + harness plugin
